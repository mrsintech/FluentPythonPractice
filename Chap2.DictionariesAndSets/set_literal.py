# to build a set we can use this syntax {1, 2, 3} but keep in mind that,
# we can't build empty set like this and we should use set()

s = {1, 2, 3, 4}
print(s)

# build frozenset
fr_s = frozenset(range(10))
print(fr_s)
